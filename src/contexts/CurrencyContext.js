import { createContext } from "react";
//this needs to be set correctly
export const CurrencyContext = createContext();
// export const CurrencyContext = createContext(document.currency);
// export const CurrencyContext = createContext({locale: document.cookies, currency: document});
